Development commands on Windows PowerShell:\n- `Get-ChildItem -Force` to inspect files\n- `Get-Content <path>` to read files\n- `git status --short` and `git diff -- <path>` for VCS checks\n- `bun run lint` to run TypeScript check and oxlint\nEntrypoint/build examples from README:\n- `bun install`\n- `bun build src/index.ts --compile --outfile=sks`\nTypical usage commands: `sks init`, `sks scan`, `sks search <query>`, `sks add <source:id>`, `sks outdated`, `sks update`.