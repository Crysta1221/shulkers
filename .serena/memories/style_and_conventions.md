Code style: strict TypeScript with ESM imports, semicolons, descriptive comments on public APIs, and narrow literal/source union types. Repository abstractions live in src/lib/repositories and expose search/getResource/getVersions/getLatestVersion/getVersionDownload methods. Commands are defined with gunshi's `define({...})` pattern and use picocolors/ora/@inquirer/prompts for CLI UX.